<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
  </head>
  <body>
    <h1>{{ title }}</h1>
    <small>{{ date | date: "%d %B %Y" }}</small>
    <div>{{ content | safe }}</div>
  </body>
</html>
